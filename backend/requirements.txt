# Phase 0 - minimal foundation. More deps added per phase; see docs/phases.md.
fastapi
uvicorn[standard]

# Phase 1 - database access (Supabase / Postgres)
psycopg[binary,pool]
python-dotenv

# Phase 2 - backend + tools
pydantic-settings
